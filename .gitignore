# ── Exclusion fichiers méta IA / agents (règle perso) ──
CLAUDE.md
**/CLAUDE.md
claude.md
**/claude.md
.claude/
**/.claude/
*claude*
*CLAUDE*
*anthropic*
*ANTHROPIC*

# ── Env UV ──
.venv/
__pycache__/
*.pyc
*.pyo
.pytest_cache/
.ipynb_checkpoints/

# ── Outputs intermédiaires lourds ──
scripts/_sandbox/
*.tmp

# ── Artefacts reproductibles (à régénérer via scripts/restore_originaux.sh) ──
# Les .ipynb originaux (52 MB) sont régénérés en dézippant Notebook_2018-2021/Notebooks.zip.
# Le zip lui-même EST committé (source de vérité) ; les .ipynb dézippés ne le sont pas.
Notebook_2018-2021/ipynb/

# ── Données volumineuses (à committer au cas par cas) ──
# BDD_DuckDB : NYC Taxi parquet (~50 MB) + bases .duckdb, régénérés par le notebook
data/BDD_DuckDB/
# Bundle Colab régénérable (zip des .ipynb, à télécharger pour Drive/Colab)
Notebook_2026/notebooks_colab.zip
# Bruit Windows (Alternate Data Stream)
*:Zone.Identifier

# ── OS / éditeurs ──
.DS_Store
Thumbs.db
.vscode/
.idea/

# ── Artefacts d'entraînement générés en exécutant les notebooks (non versionnés) ──
catboost_info/
data_mnist/
runs/
tb_logs/
model/
*.keras
*.pt
*.pth
*.h5
*.onnx
