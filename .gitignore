# ── Exclusion fichiers méta IA / agents (règle perso) ──
CLAUDE.md
**/CLAUDE.md
claude.md
**/claude.md
.claude/
**/.claude/
*claude*
*CLAUDE*
*anthropic*
*ANTHROPIC*

# ── Env UV ──
.venv/
__pycache__/
*.pyc
*.pyo
.pytest_cache/
.ipynb_checkpoints/

# ── Outputs intermédiaires lourds ──
scripts/_sandbox/
*.tmp

# ── Artefacts reproductibles (à régénérer via scripts/restore_originaux.sh) ──
# Les .ipynb originaux (52 MB) sont régénérés en dézippant 1_Old_Notebooks/Notebooks.zip.
# Le zip lui-même EST committé (source de vérité) ; les .ipynb dézippés ne le sont pas.
1_Old_Notebooks/ipynb/

# ── Données volumineuses (à committer au cas par cas) ──
# BDD_DuckDB : NYC Taxi parquet (~50 MB) + bases .duckdb, régénérés par le notebook
data/BDD_DuckDB/
# Bruit Windows (Alternate Data Stream)
*:Zone.Identifier

# ── OS / éditeurs ──
.DS_Store
Thumbs.db
.vscode/
.idea/
